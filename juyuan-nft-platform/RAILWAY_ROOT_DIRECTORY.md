# 📍 Railway Root Directory 设置位置详解

## 🎯 Root Directory 在哪里？

Root Directory（根目录）设置在 **Railway 服务的 Settings（设置）页面**中。

---

## 📋 详细步骤（带截图说明位置）

### 步骤1：进入你的服务
1. 登录 Railway Dashboard：https://railway.app
2. 点击你的项目（例如：`fantastic-magic`）
3. 在左侧服务列表中，**点击你的服务**（显示失败的那个服务）

### 步骤2：打开 Settings（设置）
在服务页面的**顶部标签栏**，你会看到：
- **Deployments**（部署）
- **Variables**（环境变量）
- **Metrics**（指标）
- **Settings**（设置）← **点击这个！**

### 步骤3：找到 Root Directory
在 Settings 页面中，向下滚动，你会看到几个部分：

#### 🔧 **Service Settings（服务设置）**
在这个部分中，你会看到：

```
┌─────────────────────────────────────┐
│ Service Settings                    │
├─────────────────────────────────────┤
│ Service Name: [你的服务名]          │
│                                     │
│ Root Directory: [这里！]            │
│                                     │
│ Build Command:                      │
│ Start Command:                      │
└─────────────────────────────────────┘
```

**Root Directory** 就在这个位置！

### 步骤4：设置 Root Directory
1. 在 **Root Directory** 输入框中
2. 输入：`backend/services/user`
3. 点击输入框外的任意位置保存（会自动保存）

---

## 🖼️ 界面位置示意图

```
Railway Dashboard
├── 项目名称 (fantastic-magic)
│   └── 服务列表
│       └── [你的服务] ← 点击这里
│           ├── Deployments (标签)
│           ├── Variables (标签)
│           ├── Metrics (标签)
│           └── Settings (标签) ← 点击这里
│               └── Service Settings
│                   └── Root Directory ← 在这里设置！
```

---

## ✅ 设置后的值

**Root Directory 应该设置为：**
```
backend/services/user
```

**不要包含：**
- ❌ 开头的 `/`（如 `/backend/services/user`）
- ❌ 结尾的 `/`（如 `backend/services/user/`）
- ✅ 正确格式：`backend/services/user`

---

## 🔍 如果找不到 Root Directory？

### 方法1：检查服务类型
- 确保你选择的是 **GitHub Repo** 类型的服务
- 如果是其他类型，可能需要重新创建服务

### 方法2：使用命令行设置
如果界面找不到，可以使用 Railway CLI：

```bash
# 安装 Railway CLI
npm i -g @railway/cli

# 登录
railway login

# 链接项目
railway link

# 设置 Root Directory
railway variables set RAILWAY_ROOT_DIRECTORY=backend/services/user
```

### 方法3：重新创建服务
1. 删除当前失败的服务
2. 点击 **"New Service"**
3. 选择 **"GitHub Repo"**
4. 选择你的仓库
5. **在创建时就会看到 Root Directory 设置选项**

---

## 📝 完整设置示例

设置 Root Directory 后，你的 Settings 页面应该显示：

```
Service Settings
├── Service Name: user-service
├── Root Directory: backend/services/user  ← 设置这个
├── Build Command: (留空，使用默认)
└── Start Command: npm start
```

---

## 🚀 设置完成后

1. **自动保存**：Railway 会自动保存设置
2. **自动重新部署**：设置保存后，Railway 会自动触发新的部署
3. **查看部署**：切换到 "Deployments" 标签查看部署进度

---

## ❓ 常见问题

### Q: Root Directory 是相对于什么的？
A: 相对于你的 Git 仓库根目录。如果你的仓库结构是：
```
仓库根目录/
├── backend/
│   └── services/
│       └── user/  ← 这是你的服务目录
```
那么 Root Directory 就是 `backend/services/user`

### Q: 设置后还是失败怎么办？
A: 
1. 检查路径是否正确（区分大小写）
2. 确认该目录下有 `package.json` 和 `Dockerfile`
3. 查看部署日志中的具体错误信息

### Q: 可以设置多个服务的 Root Directory 吗？
A: 可以！每个服务都有自己独立的 Root Directory 设置。例如：
- 服务1: `backend/services/user`
- 服务2: `backend/services/presale`
- 服务3: `frontend/web`

---

## 📞 需要帮助？

如果还是找不到，可以：
1. 截图你的 Settings 页面发给我
2. 或者告诉我你看到的选项，我来帮你定位
