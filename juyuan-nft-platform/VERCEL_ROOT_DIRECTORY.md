# 📁 Vercel Root Directory 设置指南

## ✅ 答案

### Web 前端（用户界面）

**Root Directory**: `frontend/web`

---

## 🎯 详细说明

### 为什么是 `frontend/web`？

你的项目结构是：

```
juyuan-nft-platform/
├── frontend/
│   ├── web/          ← Web 前端（用户界面）
│   │   ├── package.json
│   │   ├── next.config.js
│   │   └── src/
│   │
│   ├── admin/        ← Admin 后台（管理界面）
│   │   ├── package.json
│   │   └── src/
│   │
│   └── miniprogram/  ← 微信小程序
│
└── backend/
    └── services/
```

所以：
- **Web 前端** → Root Directory: `frontend/web`
- **Admin 后台** → Root Directory: `frontend/admin`

---

## 📋 在 Vercel 中设置

### 步骤：

1. **在项目配置页面**
   - 找到 **"Root Directory"** 字段
   - 点击旁边的 **"Edit"** 按钮

2. **输入路径**
   - 输入：`frontend/web`
   - **不要**加斜杠：
     - ✅ 正确：`frontend/web`
     - ❌ 错误：`/frontend/web`（不要开头斜杠）
     - ❌ 错误：`frontend/web/`（不要结尾斜杠）

3. **保存**
   - 点击保存或继续下一步

---

## 🎯 不同项目的 Root Directory

| 项目类型 | Root Directory |
|---------|----------------|
| **Web 前端**（用户界面） | `frontend/web` |
| **Admin 后台**（管理界面） | `frontend/admin` |
| **微信小程序** | `frontend/miniprogram` |

---

## ⚠️ 重要提示

### 1. 路径格式

- ✅ 正确：`frontend/web`
- ❌ 错误：`/frontend/web`（不要开头斜杠）
- ❌ 错误：`frontend/web/`（不要结尾斜杠）
- ❌ 错误：`./frontend/web`（不要相对路径符号）

### 2. 区分大小写

- ✅ 正确：`frontend/web`（小写）
- ❌ 错误：`Frontend/Web`（大小写敏感）

### 3. 路径必须存在

确保 GitHub 仓库中有这个目录：
- `frontend/web/package.json` 必须存在
- `frontend/web/next.config.js` 必须存在

---

## 🔍 如何验证

设置 Root Directory 后，Vercel 会：

1. **检查路径**
   - 验证 `frontend/web/package.json` 是否存在
   - 验证 `frontend/web/next.config.js` 是否存在

2. **自动检测框架**
   - 如果找到 Next.js 配置文件
   - 会自动选择 Next.js 预设

3. **显示预览**
   - 在配置页面会显示检测到的框架和设置

---

## 📝 完整配置示例

### Web 前端配置

```
Project Name: juyuan-nft-web
Framework Preset: Next.js
Root Directory: frontend/web ← 这里！
Build Command: npm run build（自动填充）
Output Directory: .next（自动填充）
```

### Admin 后台配置

```
Project Name: juyuan-nft-admin
Framework Preset: Next.js
Root Directory: frontend/admin ← 这里！
Build Command: npm run build（自动填充）
Output Directory: .next（自动填充）
```

---

## 🐛 常见错误

### 错误 1：找不到 package.json

**原因**：Root Directory 设置错误

**解决**：
- 确认路径是 `frontend/web`（不是 `web` 或 `/frontend/web`）
- 确认 GitHub 仓库中有这个目录

### 错误 2：构建失败

**原因**：Root Directory 指向了错误的目录

**解决**：
- 检查路径拼写
- 确认路径中有 `package.json` 和 `next.config.js`

---

## ✅ 快速检查清单

- [ ] Root Directory: `frontend/web`（Web 前端）
- [ ] 或 `frontend/admin`（Admin 后台）
- [ ] 没有开头斜杠 `/`
- [ ] 没有结尾斜杠 `/`
- [ ] 路径拼写正确（小写）

---

## 🎉 完成！

设置 Root Directory 为 `frontend/web` 后，Vercel 会：

- ✅ 从正确的目录构建项目
- ✅ 找到 `package.json` 和配置文件
- ✅ 自动配置构建命令
- ✅ 正确部署你的应用

---

## 📞 需要帮助？

如果设置后还是有问题：

1. **检查路径**
   - 确认 GitHub 仓库中有 `frontend/web` 目录
   - 确认目录中有 `package.json`

2. **查看构建日志**
   - 部署后查看构建日志
   - 检查是否有路径相关的错误

3. **重新设置**
   - 删除项目重新创建
   - 或修改现有项目的 Root Directory
